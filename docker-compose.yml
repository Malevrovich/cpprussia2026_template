version: '3.8'

services:
  auth-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/auth_service/configs
        BINARY_PATH: backend/auth_service/build-release/auth_service
    ports:
      - "8001:8001"
    networks:
      - app-network

  messaging-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/messaging_service/configs
        BINARY_PATH: backend/messaging_service/build-release/messaging_service
    ports:
      - "8002:8002"
    networks:
      - app-network

  notifications-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/notifications_service/configs
        BINARY_PATH: backend/notifications_service/build-release/notifications_service
    ports:
      - "8003:8003"
    networks:
      - app-network

  files-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/files_service/configs
        BINARY_PATH: backend/files_service/build-release/files_service
    ports:
      - "8004:8004"
    networks:
      - app-network

  reactions-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/reactions_service/configs
        BINARY_PATH: backend/reactions_service/build-release/reactions_service
    ports:
      - "8005:8005"
    networks:
      - app-network

  status-service:
    build:
      context: .
      dockerfile: backend/Dockerfile
      args:
        CONFIG_DIR: backend/status_service/configs
        BINARY_PATH: backend/status_service/build-release/status_service
    ports:
      - "8006:8006"
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service
      - messaging-service
      - notifications-service
      - files-service
      - reactions-service
    #   - status-service
      - frontend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge